id: no-promise-type
language: TypeScript
message: Use Effect.Effect<A, E, R> instead of Promise<T>
severity: warning
rule:
  all:
    - kind: type_identifier
    - regex: ^Promise$
    - inside:
        kind: generic_type
